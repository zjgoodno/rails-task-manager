<h1>My Tasks</h1>
<% @tasks.each do |task| %>
<p><%= check_box_tag(task[:completed]) %>
<%= link_to task.title, task_path(task[:id]) %></p>
<%= link_to "Delete", destroy_path(task[:id]), method: :delete, data: {confirm: "delete?"}%>
<% end %>

<p><%= link_to "Add New Task", new_path  %></p>
